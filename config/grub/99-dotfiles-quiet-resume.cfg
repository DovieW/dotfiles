# Keep firmware and driver diagnostics in the journal without painting them on
# the unscaled Linux console during the suspend/resume graphics handoff.
GRUB_CMDLINE_LINUX_DEFAULT="quiet splash loglevel=3 systemd.show_status=false rd.systemd.show_status=false vt.global_cursor_default=0"
