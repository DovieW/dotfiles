# Provenance

This repository was extracted from a private personal monorepo using fresh public history.

It intentionally does not preserve the original repository history, because the source repo contains private files, local cache, exports, and secret-adjacent configuration that must not enter public history.
