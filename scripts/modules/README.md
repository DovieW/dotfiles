# Setup modules

Keep setup areas isolated here if `setup.ps1` or `setup-wsl.sh` grows too large.

Each module should document whether it requires Bitwarden, GitHub auth, Windows, WSL/Linux, or elevated permissions.
